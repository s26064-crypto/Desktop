# Spring Bean
- 스프링 컨테이너(Spring Container)가 생성하고 관리하는 객체

#### 기존 자바에서는 객체를 직접 생성한다
```java
MemberService service = new MemberService();
```
- 하지만 스프링에서는 개발자가 직접 만들지 않고 스프링이 대신 객체를 생성,관리

```java
@Service
public class MemberService {

}
```
> 이렇게 하면 MemberService가 Bean으로 등록된다
- Bean = 스프링이 관리하는 객체